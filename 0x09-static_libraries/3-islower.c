#include "main.h"

/**
 * _islower - checks for lowercase character
 * @c: c is an asciii character
 * Return: 1 if lowercase
 */

int _islower(int c)
{
	for (if c >= 'a' && c <= 'z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
