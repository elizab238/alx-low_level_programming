#include <stdio.h>

/**
 * main - single digits of base 10
 * Description - code must be in main function
 * Return: 0
 */
int main(void)
{
	int n;

	for (n = 0; n < 10; n++)
		printf("%d", n);
	printf("\n");
	return (0);
}
